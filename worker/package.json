{
  "name": "prvt",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate:qrcode": "node -e \"const {execSync}=require('child_process'),fs=require('fs');const b=execSync('npx esbuild node_modules/qrcode/lib/browser.js --bundle --format=iife --global-name=QRCode --minify',{encoding:'utf8'});fs.writeFileSync('src/qrcode-src.js','// Generated by: npm run generate:qrcode\\n// Re-run after upgrading qrcode package\\nexport const QRCODE_SRC = '+JSON.stringify(b)+';\\n');console.log('wrote',b.length,'bytes');\""
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
